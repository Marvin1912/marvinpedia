apiVersion: grafana.integreatly.org/v1beta1
kind: Grafana
metadata:
  name: grafana
  labels:
    dashboards: "grafana"
  namespace: default
spec:
  config:
    log:
      mode: "console"
    security:
      admin_user: root
      admin_password: secret
